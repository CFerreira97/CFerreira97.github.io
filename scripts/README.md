# Scripts

Standalone scripts, run directly (`uv run python scripts/<name>.py`) - not part of the
installed package, not triggered by module code.

Add subfolders here as real needs come up (e.g. a yearly rollover job, a one-off local-setup
script) rather than pre-creating placeholder categories.
